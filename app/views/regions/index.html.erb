<h1>Regions</h1>

<% @regions.each do |region| %>
  <%= image_tag region.image, :class => 'thumb' %>

  <ul>
    <li> <%= link_to region.name, region_path(region.id) %></li>
    <li>About: <%= region.about %></li>


  </ul>
<% end %>
<%= link_to 'New region', new_region_path %>
